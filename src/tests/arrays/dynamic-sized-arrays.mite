export fn addition(): i32 {
    let x: [i32] = [1, 2, 3];
    return x[0] + x[1] + x[2];
}

export fn reassignment(): i32 {
    let x: [i32];
    x = [1, 2, 3];
    if (0 == (x[0] + x[1] + x[2] == 6)) return 0;
    x = [4, 5, 6];
    return x[0] + x[1] + x[2] == 15;
}
